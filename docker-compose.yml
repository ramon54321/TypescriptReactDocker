version: "3.3"
services:
  proxy:
    build:
      context: ./proxy
      dockerfile: Dockerfile
    ports:
      - "8080:80"
  redis:
    build:
      context: ./redis
      dockerfile: Dockerfile
    depends_on:
      - proxy
  web0:
    build:
      context: ./web
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    volumes:
      - ./web:/usr/src
    depends_on:
      - redis
  web1:
    build:
      context: ./web
      dockerfile: Dockerfile
    ports:
      - "3001:80"
    volumes:
      - ./web:/usr/src
    depends_on:
      - redis

